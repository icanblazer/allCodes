/* ListDemo.friendList('arpan');

String name1FromList = ListDemo.candidatesList(5);
System.debug(name1FromList);

ListDemo.randomIntegerList();
ListDemo.candidatesList(); */

List<Integer> intList = new List<Integer>();

if (!intList.isEmpty()) {
  System.debug(intList);
} else {
  System.debug('Empty List!');
}

